#!/bin/bash
LOG="rocm_setup.log"
echo "🚀 ROCm Setup gestartet: $(date)" > "$LOG"

# 🎯 Ubuntu & Kernel-Version erkennen
UBUNTU=$(lsb_release -rs)
KERNEL=$(uname -r | cut -d '-' -f1)
MAJOR=$(echo "$KERNEL" | cut -d '.' -f1)
MINOR=$(echo "$KERNEL" | cut -d '.' -f2)
CURRENT=$(echo "$MAJOR.$MINOR")

# 🧠 ROCm-Version je nach System mit awk bestimmen
ROCM_VERSION=""
if [[ "$UBUNTU" == "20.04" ]]; then
  ROCM_VERSION="5.7"

elif [[ "$UBUNTU" == "22.04" ]]; then
  if awk "BEGIN{exit !($CURRENT < 6.3)}"; then
    ROCM_VERSION="6.0"
  else
    ROCM_VERSION="6.1"
  fi

elif [[ "$UBUNTU" == "24.04" ]]; then
  if awk "BEGIN{exit !($CURRENT < 6.15)}"; then
    ROCM_VERSION="6.1"
  else
    ROCM_VERSION="6.1"
  fi

else
  ROCM_VERSION="5.7"
  echo "⚠️ Unbekannte Kombination: Ubuntu $UBUNTU + Kernel $CURRENT – fallback auf ROCm $ROCM_VERSION" | tee -a "$LOG"
fi

echo "📦 Verwende ROCm Version: $ROCM_VERSION für Ubuntu $UBUNTU + Kernel $CURRENT" | tee -a "$LOG"

# 🧹 Alte Treiber entfernen
sudo apt purge -y rocm-dkms amdgpu-pro amdgpu
sudo rm -f /etc/apt/sources.list.d/rocm.list
sudo apt autoremove -y

# 📚 ROCm-Repository hinzufügen
echo "deb [arch=amd64] https://repo.radeon.com/rocm/apt/$ROCM_VERSION/ ubuntu main" | sudo tee /etc/apt/sources.list.d/rocm.list
wget -qO - https://repo.radeon.com/rocm/rocm.gpg.key | sudo apt-key add -
sudo apt update

# 📥 ROCm-Komponenten installieren
sudo apt install -y rocm-dev hipcc clinfo rocminfo dkms

# 🔗 Kernelmodul laden
sudo modprobe amdgpu

# 🌐 Umgebungsvariablen setzen
export ROCM_PATH=/opt/rocm
export PATH=$ROCM_PATH/bin:$PATH
export LD_LIBRARY_PATH=$ROCM_PATH/lib:$ROCM_PATH/lib64:$LD_LIBRARY_PATH
export DEVICE_LIB_PATH=$ROCM_PATH/lib/amdgcn/bitcode
export HSA_OVERRIDE_GFX_VERSION=10.3.0

# 🔐 Variablen in .bashrc speichern
if ! grep -q "ROCm Setup" ~/.bashrc; then
  echo -e "\n# ROCm Setup" >> ~/.bashrc
  echo "export ROCM_PATH=/opt/rocm" >> ~/.bashrc
  echo "export PATH=\$ROCM_PATH/bin:\$PATH" >> ~/.bashrc
  echo "export LD_LIBRARY_PATH=\$ROCM_PATH/lib:\$ROCM_PATH/lib64:\$LD_LIBRARY_PATH" >> ~/.bashrc
  echo "export DEVICE_LIB_PATH=\$ROCM_PATH/lib/amdgcn/bitcode" >> ~/.bashrc
  echo "export HSA_OVERRIDE_GFX_VERSION=10.3.0" >> ~/.bashrc
fi

# 🔄 Variablen sofort aktivieren
source ~/.bashrc
echo "✅ Umgebungsvariablen aktiv!" | tee -a "$LOG"

# 🧪 Tests
echo -e "\n🔍 OpenCL-Test:" | tee -a "$LOG"
clinfo | tee -a "$LOG"

echo -e "\n🔍 ROCm Systeminfo:" | tee -a "$LOG"
rocminfo | tee -a "$LOG"

echo -e "\n✅ ROCm Setup abgeschlossen mit Version $ROCM_VERSION" | tee -a "$LOG"
